apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: install deps
        uses: docker://python
        shell: sh
        run: |
          pip install -r requirements.txt
